# Local dataminer reports

`reports/android/` is ignored and is not published. Existing local reports remain on disk. Generate and inspect reports locally; they may contain strings, endpoints or other data extracted from a third-party application.

`contentSha256` excludes generation time and comparison data. Evidence paths and confidence levels document how findings were detected; they do not establish server-side behavior.
